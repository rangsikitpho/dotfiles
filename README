Installation
===
Install MacVim for OSX
Add mvim to ~/bin
Add ~/bin to PATH

cd
git clone git@github.com:rangsikitpho/dotfiles.git
ln -nfs ~/dotfiles/vim/ ~/.vim
ln -nfs ~/.vim/vimrc ~/.vimrc
ln -nfs ~/.vim/gvimrc ~/.gvimrc

ln -nfs ~/dotfiles/screenrc ~/.screenrc

ln -nfs ~/dotfiles/rails.screen ~/.rails.screen